{
  "name": "app",
  "version": "1.0.0",
  "description": "The app the can be run via the command line or by edge.js",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jim Buck",
  "license": "MIT",
  "dependencies": {
    "socket.io-client": "^1.3.5"
  }
}
